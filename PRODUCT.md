# Product

## Register

product

## Users

Echo 面向拥有 Navidrome、Subsonic 或 OpenSubsonic 自建音乐服务的听众。他们通常长期维护自己的音乐库，重视数据控制、多线路可用性、音质、歌词与封面完整度，也会在通勤、居家、弱网和离线等不同环境中频繁使用移动端。

用户进入任意页面时的核心任务不是管理界面本身，而是尽快找到、开始、继续或控制正在播放的音乐。管理音乐库、下载、缓存、音质与提供商设置属于重要但低频的支撑任务，必须清楚可靠，不能抢走音乐内容的注意力。

## Product Purpose

Echo 将用户自己的音乐服务器变成一个完整、可信且有情绪的个人音乐产品，而不是一个展示 Subsonic 数据的通用客户端。

成功意味着用户可以快速恢复播放、探索自己的收藏、理解当前网络与离线状态，并在需要时完成复杂配置；与此同时，界面在日常听歌过程中自然退后，让封面、音乐信息和播放状态成为主角。

## Brand Personality

**沉浸、克制、敏捷。**

- **Plexamp**：借鉴它让音乐库拥有个性、让封面与播放状态驱动氛围的能力。
- **Apple Music**：借鉴它在专辑与播放器场景中对封面、空间和连续转场的处理。
- **Spotify**：借鉴它在长列表、快速操作和高频浏览中的信息效率。

Echo 不直接复刻其中任何一个产品。它应当呈现出属于自建音乐库用户的安静自信、可靠感与个人性。

## Anti-references

- 不做默认 Material 3 组件陈列页，也不接受只通过换色、圆角和阴影完成的 Material 换肤。
- 不做 Apple Music、Spotify 或 Plexamp 的直接复刻。
- 不把 `feature-glass-like` 或任何历史视觉实验分支作为实现来源。外部产品与历史分支只提供问题空间参考，不能复用其组件代码、主题配置、图标别名映射或页面补丁。
- 不让整个应用壳层随每次切歌持续变色；动态专辑色只服务于与当前内容直接相连的局部场景。
- 不把毛玻璃、渐变光球、发光阴影或大圆角卡片铺满每个页面。
- 不用装饰性动效打断高频任务，不使用需要等待的编排式页面入场。
- 不把设置、下载、缓存和长列表做成同质化 `ListTile + Divider` 或层层嵌套卡片。
- 不为了追求个性而发明陌生的返回、确认、选择、搜索和表单交互。
- 不牺牲单手操作、动态字体、弱网反馈、离线可用性和长列表性能。

## Design Principles

1. **音乐是视觉光源。** 封面与当前内容可以照亮播放器、MiniPlayer、媒体详情头部及其相邻弹层；普通浏览和设置界面保持稳定中性。
2. **界面退后，任务前进。** 每个页面只建立一个主要焦点，减少无意义容器，让标题、封面、列表和关键操作直接形成层级。
3. **空间连续胜过装饰动画。** 动效用于解释页面、播放器、封面和状态之间的关系，不能仅仅为了显得活跃。
4. **移动端效率优先。** 高频操作单手可达，触控目标充足，长列表紧凑但不拥挤，核心操作不藏在多层菜单中。
5. **真实状态必须同样精致。** 加载、空内容、弱网、离线、失败、禁用和部分数据都属于产品本体，不是实现后的补丁。
6. **熟悉的行为，自有的表达。** 保留用户已经理解的移动端交互语义，通过 Echo 的排版、色彩、图标、触感和空间关系形成品牌，而不是重造标准操作。

## Accessibility & Inclusion

- 以 WCAG 2.2 AA 对比度为最低目标，正文与占位文本达到 4.5:1，大字号文本与非文本关键控件达到 3:1。
- 所有主要触控目标不小于 48dp，并为手势操作提供可发现、可点击的替代入口。
- 跟随系统字体缩放，关键流程在 200% 文本缩放下仍可完成，不依赖截断文本传递唯一信息。
- 跟随系统明暗模式，并支持手动选择；高对比需求下仍保持清楚的焦点、边界和选中状态。
- 尊重减少动态效果设置。共享元素和场景转场退化为短交叉淡化或即时切换，不影响任务完成。
- 状态不能只靠颜色表达。选中、错误、下载、离线和播放状态同时使用图标、文字、形状或语义标签。
- 为屏幕阅读器提供稳定的阅读顺序、明确的操作名称、当前播放状态和进度描述。
- 触觉反馈只作为增强，不作为确认操作是否成功的唯一方式。
